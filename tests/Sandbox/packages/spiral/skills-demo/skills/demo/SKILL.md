# Demo (from spiral/skills-demo)

This skill belongs to a donor whose vendor (`spiral/*`) is in the plugin's
built-in trusted list. Acceptance tests use it to prove that
`trusted-replace: true` correctly disables the built-in defaults.
