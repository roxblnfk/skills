---
name: hidden-claude
description: A skill shipped under .claude/skills, discoverable only via recursive scanning.
---

# Hidden Claude skill

This skill lives under `.claude/skills/`, not the conventional `skills/` root.
It exists to prove auto-discovery finds `SKILL.md` outside `skills/`.
